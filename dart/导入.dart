import '导出.dart';
void main(){
 /*  Person p=new Person();
  p.sayName(); */
  //实例化对象的时候，构造函数会自动触发
  /* Person p=new Person("xiang",20);
  print(p.name); */
  Student s = new Student("cheng",20,"lol-code");
  print(s.skill);
}